import { JsonStorage } from "../ttnet/p/q"; import List from '@ohos.util.List'; export declare class OhosNetworkLibrary { private static readonly TAG; static eventHistory: List<string>; private cronetNetworkChangeNotifier?; private defaultConnection; private mRegistered; private default_net_id; private default_net_type; private default_proxy; private default_availability; private default_has_vpn; private current_net_id; private current_net_type; private current_availability; private current_has_vpn; private current_proxy; initNotifyCronet(f7: boolean): void; private timerId; setEnableRefreshProxy(enable: boolean, d7: number): void; private registerEvent; private notifyCronetConnected; private notifyCronetDisconnect; private unregisterEvent; private refreshDefaultNetState; private getNetBearTypeFromNetCapabilities; private isNetworkAvailable; private hasVPN; private refreshProxy; private current_all_nets; private refreshAllNets; getCurrentState(): Promise<string>; private isNotifyOnInit; onTNCConfigUpdate(d6: JsonStorage): void; } 