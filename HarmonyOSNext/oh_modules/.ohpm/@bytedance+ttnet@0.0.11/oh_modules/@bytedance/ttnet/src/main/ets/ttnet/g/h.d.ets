import { Header } from "../a/d"; import List from '@ohos.util.List'; export declare class RequestRetryResult { private requestRetryEnabled; private addRequestHeaders; constructor(enabled: boolean, headers: Map<string, string>); isRequestRetryEnabled(): boolean; getAddRequestHeaders(): Map<string, string>; } export declare abstract class BDTuringRetryCallback { abstract onCallToRetryRequestByTuringHeader(responseHeaders: List<Header>): Promise<RequestRetryResult>; } export declare class BDTuringRetry { private static sRetryRequestCallback?; static readonly BDTURING_RETRY_HEADER: string; static readonly BDTURING_BYPASS_HEADER: string; static readonly BDTURING_VERIFY_HEADER: string; static setRetryRequestCallback(callback: BDTuringRetryCallback): void; static getRetryRequestCallback(): BDTuringRetryCallback | undefined; } 