import { TypedOutput } from "./u"; export declare class TypedFile extends TypedOutput { private static readonly TAG; private readonly uploadFileName; private uploadFileLength; private readonly contentType; private readIndex; private uploadFile?; constructor(fileName: string, v13: number, mimeType?: string); fileName(): string; mimeType(): string; length(): number; getBuffer(maxSize: number): Promise<[ ArrayBuffer, boolean ]>; } 