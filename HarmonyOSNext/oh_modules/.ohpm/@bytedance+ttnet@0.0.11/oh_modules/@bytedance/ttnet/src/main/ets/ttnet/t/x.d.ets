import { TypedOutput } from "./u"; export declare class TypedArrayBuffer extends TypedOutput { private static readonly TAG; protected count: number; protected myBuffer: ArrayBuffer; private readIndex; private contentType; constructor(buffer: ArrayBuffer, mimeType?: string); fileName(): string; mimeType(): string; length(): number; getBuffer(maxSize: number): Promise<[ ArrayBuffer, boolean ]>; } 