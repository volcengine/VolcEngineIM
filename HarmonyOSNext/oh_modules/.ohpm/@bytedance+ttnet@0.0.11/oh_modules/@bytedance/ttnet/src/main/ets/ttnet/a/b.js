import List from '@ohos.util.List'; export class Response { constructor(url, s8, headers, body, t8, u8) { this.url = url; this.httpStatusCode = s8; this.headers = headers; this.body = body; this.logId = t8; this.requestLog = u8; } getUrl() { return this.url; } getHttpStatusCode() { return this.httpStatusCode; } getHeaders() { return this.headers; } getBody() { return this.body; } getLogId() { return this.logId; } getRequestLog() { return this.requestLog; } getHeadersWithKey(key) { let result = new List(); for (let header of this.headers) { if (header.getName().toLowerCase() === key.toLowerCase()) { result.add(header); } } return result; } getFirstHeadersWithKey(key) { for (let header of this.headers) { if (header.getName().toLowerCase() === key.toLowerCase()) { return header; } } return undefined; } } 