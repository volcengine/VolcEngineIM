import { TTEventListener } from "../g/c1"; import List from '@ohos.util.List'; import { TTLogger } from "../p/l1"; export class TTEventListenerManager extends TTEventListener { constructor() { super(...arguments); this.listenerList = new List(); } registerEventListener(listener) { this.listenerList.add(listener); } removeEventListener(listener) { this.listenerList.remove(listener); } onInitSucceed() { TTLogger.debug(TTEventListenerManager.TAG, "onInitSucceed"); for (let index = 0; index < this.listenerList.length; ++index) { this.listenerList[index].onInitSucceed(); } } onInitFailed(c12, d12) { TTLogger.debug(TTEventListenerManager.TAG, "onInitFailed"); for (let index = 0; index < this.listenerList.length; ++index) { this.listenerList[index].onInitFailed(c12, d12); } } } TTEventListenerManager.TAG = TTEventListenerManager.name; 