import { ISDKContext } from "../d/f1"; import BaseEvent from "./i"; import { EventDbStore } from "../y1/m2"; import { bundleManager } from '@kit.AbilityKit'; export default class EventProcessor { private sdkContext; private eventDbStore; eventList: BaseEvent[]; isEventDbReady: boolean; lastProcessTaskId: number; eventId: number; blockList: string[]; bundleInfo: bundleManager.BundleInfo; constructor(a2: ISDKContext); monitorEventDbReady(): void; monitorBlockListUpdate(): void; receive(x1: BaseEvent, y1?: boolean): void; getProcessTaskDelay(): number; postProcessTask(v1?: number): void; private process; fillEventParam(s1: BaseEvent): void; fillEventParamAsync(r1: BaseEvent): Promise<void>; getEventDbStore(): EventDbStore; } 