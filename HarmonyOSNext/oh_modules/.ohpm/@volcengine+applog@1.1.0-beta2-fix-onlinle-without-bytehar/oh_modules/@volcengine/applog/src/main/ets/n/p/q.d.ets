import { ISDKContext } from "../../b/d/f1"; import BasePlugin from "../o"; import ABTestItem from "./n3"; declare class ABTest extends BasePlugin { static pluginName: string; private abTestConfig; private abTestWorker; private exposedVids; private abTestEnabled; private hasInit; constructor(h21: ISDKContext); enablePlugin(): void; private applyAB; disablePlugin(): void; isABTestEnabled(): boolean; pluginName(): string; private registerUserChangeListener; registerDidRequestListener(): void; private init; getAbSdkVersion(): string; getAbConfig<i64>(b21: string, c21: i64): i64; private addExposedVid; private sendABExposure; getAllAbTestConfigs(): Record<string, ABTestItem>; private setABTestConfig; private updateVids; } export default ABTest; 