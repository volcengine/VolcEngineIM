import { ISDKContext } from "../../b/d/f1"; import BasePlugin from "../o"; import { UIAbility } from '@kit.AbilityKit'; import Launch from "./w3"; import Terminate from "./y3"; import { PluginName } from "../../b/k3"; import PageInfo from "./x3"; export default class Session extends BasePlugin { static pluginName: PluginName; sessionId: string; sessionIndex: number; resumeFromBackground: boolean; runTimeLaunch: Launch | null; lastTerminate: Terminate | null; currPageInfo: PageInfo | null; constructor(k31: ISDKContext); enablePlugin(): void; disablePlugin(): void; pluginName(): string; checkFistLaunch(): void; updateUIAbilityInfo(j31: UIAbility): void; startSession(): void; launch(i31?: string): void; terminate(): void; } 