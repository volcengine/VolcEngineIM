import { HeaderValue } from "../../k3"; export interface IHeaderManager extends IReadOnlyHeaderManager { updateHeader<T extends HeaderValue>(key: string, value: T, isNeedSaveToKv?: boolean): void; getValue<T extends HeaderValue>(key: string, defaultValue: T): T; getHeader(): Map<string, HeaderValue>; prepare(): Promise<boolean>; } export interface IReadOnlyHeaderManager { getValue<T extends HeaderValue>(key: string, defaultValue: T): T; } 