import styled from 'styled-components';

export default styled.span`
  display: inline-block;
  height: auto;
  font-size: 12px;
  line-height: 20px;
  white-space: nowrap;

  .im-tag-text {
    margin-left: 5px;
  }
`;
