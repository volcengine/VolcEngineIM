import styled from 'styled-components';

export default styled.span`
  display: flex;
  align-items: center;
  height: auto;
  font-size: 12px;
  line-height: 16px;
  color: rgba(22, 24, 35, 0.34);
  line-height: 20px;
  white-space: nowrap;

  .im-tag-text {
    margin-left: 5px;
  }
`;
